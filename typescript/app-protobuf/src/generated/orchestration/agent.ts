// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.19.1
// source: orchestration/agent.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";

export const protobufPackage = "guru.orchestration.agent";

export interface RegisterRequest {
  serverId: string;
  runningRevision: bigint;
}

export interface RegisterReply {
  refreshKey: string;
}

export interface WatchConfigRequest {
}

export interface ConfigRevision {
  revision: bigint;
  toml: string;
}

export interface AckConfigRequest {
  revision: bigint;
  error?: string | undefined;
}

export interface AckConfigReply {
}

function createBaseRegisterRequest(): RegisterRequest {
  return { serverId: "", runningRevision: 0n };
}

export const RegisterRequest: MessageFns<RegisterRequest> = {
  encode(message: RegisterRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverId !== "") {
      writer.uint32(10).string(message.serverId);
    }
    if (message.runningRevision !== 0n) {
      if (BigInt.asIntN(64, message.runningRevision) !== message.runningRevision) {
        throw new globalThis.Error("value provided for field message.runningRevision of type int64 too large");
      }
      writer.uint32(16).int64(message.runningRevision);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serverId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.runningRevision = reader.int64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterRequest {
    return {
      serverId: isSet(object.serverId)
        ? globalThis.String(object.serverId)
        : isSet(object.server_id)
        ? globalThis.String(object.server_id)
        : "",
      runningRevision: isSet(object.runningRevision)
        ? BigInt(object.runningRevision)
        : isSet(object.running_revision)
        ? BigInt(object.running_revision)
        : 0n,
    };
  },

  toJSON(message: RegisterRequest): unknown {
    const obj: any = {};
    if (message.serverId !== "") {
      obj.serverId = message.serverId;
    }
    if (message.runningRevision !== 0n) {
      obj.runningRevision = message.runningRevision.toString();
    }
    return obj;
  },

  create(base?: DeepPartial<RegisterRequest>): RegisterRequest {
    return RegisterRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RegisterRequest>): RegisterRequest {
    const message = createBaseRegisterRequest();
    message.serverId = object.serverId ?? "";
    message.runningRevision = (object.runningRevision !== undefined && object.runningRevision !== null)
      ? BigInt(object.runningRevision)
      : 0n;
    return message;
  },
};

function createBaseRegisterReply(): RegisterReply {
  return { refreshKey: "" };
}

export const RegisterReply: MessageFns<RegisterReply> = {
  encode(message: RegisterReply, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.refreshKey !== "") {
      writer.uint32(10).string(message.refreshKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterReply {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.refreshKey = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterReply {
    return {
      refreshKey: isSet(object.refreshKey)
        ? globalThis.String(object.refreshKey)
        : isSet(object.refresh_key)
        ? globalThis.String(object.refresh_key)
        : "",
    };
  },

  toJSON(message: RegisterReply): unknown {
    const obj: any = {};
    if (message.refreshKey !== "") {
      obj.refreshKey = message.refreshKey;
    }
    return obj;
  },

  create(base?: DeepPartial<RegisterReply>): RegisterReply {
    return RegisterReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RegisterReply>): RegisterReply {
    const message = createBaseRegisterReply();
    message.refreshKey = object.refreshKey ?? "";
    return message;
  },
};

function createBaseWatchConfigRequest(): WatchConfigRequest {
  return {};
}

export const WatchConfigRequest: MessageFns<WatchConfigRequest> = {
  encode(_: WatchConfigRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WatchConfigRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWatchConfigRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): WatchConfigRequest {
    return {};
  },

  toJSON(_: WatchConfigRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<WatchConfigRequest>): WatchConfigRequest {
    return WatchConfigRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<WatchConfigRequest>): WatchConfigRequest {
    const message = createBaseWatchConfigRequest();
    return message;
  },
};

function createBaseConfigRevision(): ConfigRevision {
  return { revision: 0n, toml: "" };
}

export const ConfigRevision: MessageFns<ConfigRevision> = {
  encode(message: ConfigRevision, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.revision !== 0n) {
      if (BigInt.asIntN(64, message.revision) !== message.revision) {
        throw new globalThis.Error("value provided for field message.revision of type int64 too large");
      }
      writer.uint32(8).int64(message.revision);
    }
    if (message.toml !== "") {
      writer.uint32(18).string(message.toml);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigRevision {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigRevision();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.revision = reader.int64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.toml = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfigRevision {
    return {
      revision: isSet(object.revision) ? BigInt(object.revision) : 0n,
      toml: isSet(object.toml) ? globalThis.String(object.toml) : "",
    };
  },

  toJSON(message: ConfigRevision): unknown {
    const obj: any = {};
    if (message.revision !== 0n) {
      obj.revision = message.revision.toString();
    }
    if (message.toml !== "") {
      obj.toml = message.toml;
    }
    return obj;
  },

  create(base?: DeepPartial<ConfigRevision>): ConfigRevision {
    return ConfigRevision.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ConfigRevision>): ConfigRevision {
    const message = createBaseConfigRevision();
    message.revision = (object.revision !== undefined && object.revision !== null) ? BigInt(object.revision) : 0n;
    message.toml = object.toml ?? "";
    return message;
  },
};

function createBaseAckConfigRequest(): AckConfigRequest {
  return { revision: 0n, error: undefined };
}

export const AckConfigRequest: MessageFns<AckConfigRequest> = {
  encode(message: AckConfigRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.revision !== 0n) {
      if (BigInt.asIntN(64, message.revision) !== message.revision) {
        throw new globalThis.Error("value provided for field message.revision of type int64 too large");
      }
      writer.uint32(8).int64(message.revision);
    }
    if (message.error !== undefined) {
      writer.uint32(18).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AckConfigRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAckConfigRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.revision = reader.int64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AckConfigRequest {
    return {
      revision: isSet(object.revision) ? BigInt(object.revision) : 0n,
      error: isSet(object.error) ? globalThis.String(object.error) : undefined,
    };
  },

  toJSON(message: AckConfigRequest): unknown {
    const obj: any = {};
    if (message.revision !== 0n) {
      obj.revision = message.revision.toString();
    }
    if (message.error !== undefined) {
      obj.error = message.error;
    }
    return obj;
  },

  create(base?: DeepPartial<AckConfigRequest>): AckConfigRequest {
    return AckConfigRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AckConfigRequest>): AckConfigRequest {
    const message = createBaseAckConfigRequest();
    message.revision = (object.revision !== undefined && object.revision !== null) ? BigInt(object.revision) : 0n;
    message.error = object.error ?? undefined;
    return message;
  },
};

function createBaseAckConfigReply(): AckConfigReply {
  return {};
}

export const AckConfigReply: MessageFns<AckConfigReply> = {
  encode(_: AckConfigReply, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AckConfigReply {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAckConfigReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): AckConfigReply {
    return {};
  },

  toJSON(_: AckConfigReply): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<AckConfigReply>): AckConfigReply {
    return AckConfigReply.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<AckConfigReply>): AckConfigReply {
    const message = createBaseAckConfigReply();
    return message;
  },
};

/**
 * The worker control plane. `Register` is authenticated with an operator API
 * key (`x-api-key`, Maintainer+); `WatchConfig` and `AckConfig` are
 * authenticated with the dynamic refresh key (`x-refresh-key`) that
 * `Register` returned.
 */
export type WorkerAgentDefinition = typeof WorkerAgentDefinition;
export const WorkerAgentDefinition = {
  name: "WorkerAgent",
  fullName: "guru.orchestration.agent.WorkerAgent",
  methods: {
    register: {
      name: "Register",
      requestType: RegisterRequest as typeof RegisterRequest,
      requestStream: false,
      responseType: RegisterReply as typeof RegisterReply,
      responseStream: false,
      options: {},
    },
    watchConfig: {
      name: "WatchConfig",
      requestType: WatchConfigRequest as typeof WatchConfigRequest,
      requestStream: false,
      responseType: ConfigRevision as typeof ConfigRevision,
      responseStream: true,
      options: {},
    },
    ackConfig: {
      name: "AckConfig",
      requestType: AckConfigRequest as typeof AckConfigRequest,
      requestStream: false,
      responseType: AckConfigReply as typeof AckConfigReply,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface WorkerAgentServiceImplementation<CallContextExt = {}> {
  register(request: RegisterRequest, context: CallContext & CallContextExt): Promise<DeepPartial<RegisterReply>>;
  watchConfig(
    request: WatchConfigRequest,
    context: CallContext & CallContextExt,
  ): ServerStreamingMethodResult<DeepPartial<ConfigRevision>>;
  ackConfig(request: AckConfigRequest, context: CallContext & CallContextExt): Promise<DeepPartial<AckConfigReply>>;
}

export interface WorkerAgentClient<CallOptionsExt = {}> {
  register(request: DeepPartial<RegisterRequest>, options?: CallOptions & CallOptionsExt): Promise<RegisterReply>;
  watchConfig(
    request: DeepPartial<WatchConfigRequest>,
    options?: CallOptions & CallOptionsExt,
  ): AsyncIterable<ConfigRevision>;
  ackConfig(request: DeepPartial<AckConfigRequest>, options?: CallOptions & CallOptionsExt): Promise<AckConfigReply>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export type ServerStreamingMethodResult<Response> = { [Symbol.asyncIterator](): AsyncIterator<Response, void> };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
